# ================================================
# TradeX - PLATFORM ONBOARDING PORTAL
# Environment Configuration Template
# ================================================
# Copy this file to .env and fill in your values
# DO NOT commit .env to version control
# ================================================

# -------------------- APPLICATION --------------------
# Environment: development, staging, production
APP_ENV=development

# Secret key for session encryption (CHANGE IN PRODUCTION!)
# Generate with: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=dev-secret-key-change-in-production

# -------------------- DATABASE --------------------
# Database type: sqlite (dev) or mssql (production)
DB_TYPE=sqlite

# SQLite Configuration (Development)
SQLITE_DB_PATH=instance/tradex.db

# Azure SQL Configuration (Production)
# Uncomment and configure for production deployment
# DB_TYPE=mssql
# MSSQL_SERVER=your-server.database.windows.net
# MSSQL_DATABASE=tradex_onboarding
# MSSQL_USERNAME=admin_user
# MSSQL_PASSWORD=your-strong-password
# MSSQL_DRIVER=ODBC Driver 18 for SQL Server
#
# Alternatively, you can provide a full connection string (preferred for Azure)
# Copy the "ADO.NET" connection string from Azure SQL resource and paste below
# AZURE_SQL_CONNECTIONSTRING=Server=tcp:your-server.database.windows.net,1433;Database=tradex_onboarding;User ID=admin_user;Password=your-strong-password;Encrypt=true;TrustServerCertificate=false;Connection Timeout=30

# Advanced: Override with a complete SQLAlchemy URI if needed
# Example (ensure driver value matches installed ODBC driver)
# SQLALCHEMY_DATABASE_URI=mssql+pyodbc://admin_user:your-strong-password@your-server.database.windows.net/tradex_onboarding?driver=ODBC+Driver+18+for+SQL+Server&Encrypt=yes

# Database Debugging (set to true to log SQL queries)
SQLALCHEMY_ECHO=false

# -------------------- AUTHENTICATION --------------------
# Admin Emails (comma-separated, no spaces)
# Users with these emails will have admin access
ADMIN_EMAILS=admin@tradex.com

# Session Configuration
SESSION_TYPE=filesystem
# Session lifetime in seconds (3600 = 1 hour, 7200 = 2 hours)
PERMANENT_SESSION_LIFETIME=3600

# -------------------- AZURE AD / OAUTH (Production) --------------------
# Azure Active Directory (Entra ID) OAuth Configuration
# Uncomment and configure for production OAuth authentication

# OAuth Authority URL (tenant-specific)
# OAUTH_AUTHORITY=https://login.microsoftonline.com/<your-tenant-id>/v2.0

# Application (client) ID from Azure AD App Registration
# OAUTH_CLIENT_ID=your-application-client-id

# Client Secret from Azure AD App Registration
# OAUTH_CLIENT_SECRET=your-client-secret

# Redirect URI (must match Azure AD App Registration)
# OAUTH_REDIRECT_URI=https://your-domain.com/auth/callback

# OAuth Scopes (comma or space-separated)
# OAUTH_SCOPES=openid,profile,email,User.Read

# -------------------- AZURE COMMUNICATION SERVICES --------------------
# Azure Communication Services for Email Notifications
# Create ACS resource in Azure Portal and copy connection string

# ACS Connection String
# ACS_CONNECTION_STRING=endpoint=https://<resource-name>.communication.azure.com/;accesskey=<access-key>

# Sender Email Address (must be verified in ACS)
# ACS_SENDER=DoNotReply@tradex.com

# -------------------- SMTP EMAIL (Fallback) --------------------
# SMTP Configuration for email notifications (alternative to ACS)
# Uncomment if using SMTP instead of Azure Communication Services

# SMTP Server
# EMAIL_SMTP_SERVER=smtp.office365.com

# SMTP Port (587 for TLS, 465 for SSL, 25 for unencrypted)
# EMAIL_SMTP_PORT=587

# SMTP Username
# EMAIL_USERNAME=noreply@tradex.com

# SMTP Password
# EMAIL_PASSWORD=your-smtp-password

# Use TLS (true/false)
# EMAIL_USE_TLS=true

# -------------------- LOGGING & MONITORING --------------------
# Application Insights (Optional)
# APPLICATIONINSIGHTS_CONNECTION_STRING=InstrumentationKey=your-key;IngestionEndpoint=https://...

# Log Level: DEBUG, INFO, WARNING, ERROR, CRITICAL
# LOG_LEVEL=INFO

# -------------------- FEATURE FLAGS --------------------
# Enable/disable specific features
# ENABLE_EMAIL_NOTIFICATIONS=true
# ENABLE_AUDIT_LOGGING=true
# ENABLE_OAUTH=false

# ================================================
# NOTES:
# - All commented lines are optional or production-only
# - For local development, only uncomment what you need
# - For production, configure all security-related settings
# - Store sensitive values in Azure Key Vault for production
# ================================================
